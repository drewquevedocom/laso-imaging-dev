

# Remove "Achieva 3.0T Mobile MRI" from Inventory

The product exists as a rental item in the `inventory` table. Single action needed:

**Delete the record** with ID `bda24187-6df6-48aa-9608-bd89419a22f9` from the `inventory` table using a DELETE query via the data tool.

No code file changes required — this product only exists in the database, not in any static code files.

